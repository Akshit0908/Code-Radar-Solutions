#include <stdio.h>
int main() {
    char i;
    scanf("%c",&i);
    if(i>='a' && i<='z'){
        printf("Lowercase");
    }
    else if (){
        printf("Uppercase");
    }
    return 0;
}